node_modules/
dist/
*.log
.DS_Store

# Secrets — never commit. `.env.example` is the committed template.
.env
.env.*
!.env.example

# Generated proof artifacts
conformance-report.json

# Internal planning documents for the grant application; not part of the
# published artifact.
build_spec.md
context.md

# Draft of an upstream bug report, for review before posting; not part of the artifact.
UPSTREAM-ISSUE-DRAFT.md

# Cached probe artifacts fetched by scripts/collect-probe-results.sh
.probe-results/
.vercel
.vercel
.env*
